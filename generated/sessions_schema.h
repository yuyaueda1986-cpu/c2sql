/* AUTO-GENERATED by tools/c2sql_gen.py from sessions.json. DO NOT EDIT.
 * Regenerate with: python3 tools/c2sql_gen.py sessions.json -o <dir>
 */
#ifndef SESSIONS_SCHEMA_H
#define SESSIONS_SCHEMA_H

#include "c2sql.h"

#include <stddef.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef struct Session {
    int32_t id;
    char token[64];
} Session;

#define SESSIONS_COL_COUNT   2u
#define SESSIONS_MAX_RECORDS 3

/* Column definitions for table "sessions" (deep-copied on register). */
extern const SqlRDBColumnDef SESSIONS_COLS[SESSIONS_COL_COUNT];

/* Register the "sessions" schema (CREATE TABLE / schema check). */
SqlRDBResult RegisterSessions(SqlRDBHandle *h);

/* Capacity-guarded write (enforce_max_records). Rejects an INSERT that
 * would exceed SESSIONS_MAX_RECORDS with
 * SQL_RDB_ERR_CAPACITY_EXCEEDED. Best-effort: the count and the write are
 * not performed as a single transaction. UPSERT of an existing key is
 * always allowed (it does not grow the table). */
SqlRDBResult WriteSessionsGuarded(SqlRDBHandle *h, const Session *row,
                       const uint8_t *null_map);

#ifdef __cplusplus
}
#endif

#endif /* SESSIONS_SCHEMA_H */
